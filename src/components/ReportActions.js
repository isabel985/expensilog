import React from 'react';

const ReportActions = () => {
  return (
    <div className='report-actions'>
    <button id='newReport' className='new-report'>New Report</button>
    <button id='exportReport' className='export-report'>Export to</button>
    </div>
  );
}

export default ReportActions;